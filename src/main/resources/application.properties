# Mongo DB connection settings
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=car_database
prometheus.namespace=PROMETHEUS_PREFIX;

prometheus.hostname=PROMETHEUS_HOSTNAME;

prometheus.port = 3030
